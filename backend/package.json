{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "concurrently \"tsc --watch\" \"nodemon -q dist/index.js\"",
    "docker:setup": "docker create --name medorg-db --env-file ./.env -p \"5432:5432\" postgres",
    "docker:start": "docker start medorg-db",
    "docker:stop": "docker stop medorg-db",
    "prisma:studio": "prisma studio"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.4.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.18",
    "@types/node": "^20.8.3",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1",
    "prisma": "^5.4.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.2.2"
  }
}
